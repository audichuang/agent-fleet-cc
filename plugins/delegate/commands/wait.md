---
description: Wait for a delegate job to finish
argument-hint: "<job-id> [--timeout-s <n>] [--json]"
---

Run and relay:

```
node "${CLAUDE_PLUGIN_ROOT}/scripts/delegate-companion.mjs" wait $ARGUMENTS
```
